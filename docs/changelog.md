# Changelog (high level)

## v2.4.43-PATCH (FinalPolish)
- Stable lock screen enforcement path with SystemData refresh helper (reboot recommended in enforced/system scenarios)
- WPF UI improvements:
  - visible checkbox states (tri-state: dash/off/on) with tooltips
  - improved checkbox contrast
  - cyan-themed scrollbars
- Standalone detachment (relaunch) behavior
- Snapshots:
  - snapshot before apply
  - restore snapshot
  - undo last apply
- Quick apply buttons:
  - profile / lock / wallpaper
- Portable mode:
  - enable/disable from UI
  - data stored next to script in portable mode
- Labs/IT:
  - health check + optional auto-fix
- Convenience:
  - header badges (Admin/Mode)
  - copy log
  - open snapshots
  - reset “No change”
